{
  "servers": {
    "seedance": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/seedance-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
